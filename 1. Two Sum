class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        li = []
        for i in range(len(nums)):
            for j in range(i+1,len(nums)):
               s = nums[i]+nums[j]
               if s == target:
                li.append(i)
                li.append(j)
        return li
